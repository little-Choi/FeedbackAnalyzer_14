# 작업 보고서 — 레거시 P0/P1 결함 수정 및 테스트 Green

- **보고서 번호**: 007
- **작성일**: 2026-05-22
- **브랜치**: green
- **커밋(작성 시점)**: 469fede (push 전)

## 1. 작업 개요

[docs/qa_analysis.md](../docs/qa_analysis.md)와 [docs/test_plan.md](../docs/test_plan.md)를 기준으로 레거시 `src/cpp/`의 P0/P1 기능 결함을 수정했습니다. 기존 Catch2 64케이스는 `[known-fail]`로 버그 동작을 고정한 상태였으며, 이번 세션에서 프로덕션 코드를 고친 뒤 테스트 기대값·헬퍼를 동기화하여 **64 test cases · 135 assertions 전부 Green**을 달성했습니다. [README.md](../README.md)에는 케이스별 체크박스와 레거시 수정 요약 표를 반영했습니다.

## 2. 변경·산출물 목록

| 구분 | 경로/항목 | 설명 |
|------|-----------|------|
| 수정 | `src/cpp/Filters.h`, `Filters.cpp` | `main` 카테고리 필터 정합; `S_KEYWORDS` 제거 → `Constants::SENTIMENT_KEYWORDS` 단일화 |
| 수정 | `src/cpp/Constants.cpp` | 부정 키워드 `늦`, `늦다` 추가 |
| 수정 | `src/cpp/main.cpp` | CSV `text` 컬럼·헤더 없음 1행 유지; 업로드 후 통계; `escapeHtml` apostrophe |
| 수정 | `tests/unit/test_*.cpp` (filters, text_analyzer, html_escape) | `[known-fail]` 해제·기대값 갱신 |
| 수정 | `tests/integration/test_csv_behavior.cpp`, `test_http_exceptions.cpp` | CSV·HTTP-H04b 기대값 갱신 |
| 수정 | `tests/integration/LegacyCsv.h`, `LegacyHtml.h` | main.cpp 로직과 동기화 |
| 수정 | `tests/fixtures/HttpFixture.h` | 업로드 CSV·통계 렌더 동기화 |
| 수정 | `README.md` | 테스트 케이스별 [x], 레거시 수정 요약, qa_analysis 링크 |
| 신규 | `Report/007-20260522-legacy-p0-p1-fixes.md` | 본 보고서 |
| 신규 | `Prompting/007-...-Prompt.md`, `Prompting_user/007-...-User.md` | 대화 저장 |
| 제외 | `build/`, `build-full/`, `build-test/` | 로컬 빌드 산출물 |

### 해제된 known-fail 매핑

| TEST_F | 수정 내용 |
|--------|-----------|
| FI-F05 | 품질 필터 → `main` 키워드 매칭 |
| FI-F06 | 중립 필터 ↔ 통계 사전 일치 |
| FI-F07b | `늦`/`늦다` → `늦었어요` 부정 |
| CSV-R02 | `id,text` → `text` 컬럼 사용 |
| CSV-NH01 | 헤더 없는 CSV 첫 행 유지 |
| HTTP-H04b | 업로드 후 `sent`/`kw` 통계 |
| HTML-02 | `'` → `&#39;` |
| TA-S07 | `늦었어요` → 부정 (통계·필터 일치) |

## 3. 주요 결정·이슈

- **결정**: Phase 2(키워드 단일화)를 `Filters`에서 선행 적용 — `initFilterKeywords()`는 no-op 유지(호출부 호환).
- **결정**: 카테고리 필터는 `TextAnalyzer::kw`와 동일하게 `CATEGORY_KEYWORDS[cat]["main"]`만 검사.
- **결정**: TA-S04(긍·부정 혼재→긍정)는 if-else 순서 유지 — 버그 문서화가 아닌 명시 규칙으로 Green 유지.
- **미해결**: `fil_data` 전역, CSV 다운로드 이스케이프, `FileHandler` 스텁, `renderPage` God function — 리팩토링 범위.

## 4. 테스트·검증

```text
cmake --build build-full
build-full\tests\feedback_analyzer_tests.exe
→ All tests passed (135 assertions in 64 test cases)
```

- 이전(006): 64 cases / 136 assertions, `[known-fail]` 8건은 버그 기준선
- 이후(007): `[known-fail]` 태그 제거, 프로덕션 수정 반영

## 5. 다음 단계

1. `docs/qa_analysis.md` §2 결함 표를 “수정됨” 상태로 동기화.
2. gcov/lcov로 line coverage 90% 측정 (`-DENABLE_COVERAGE=ON`).
3. Session → Repository, `fil_data` 제거 등 Phase 3~7 리팩토링.
